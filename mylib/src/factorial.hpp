#include <vector>
#include <tuple>

std::tuple<std::vector<long long>, std::vector<long long> > calc_factorial(
    const int n, const long long MOD)
{
    std::vector<long long> fact, ifact;
    return {fact, ifact};
}